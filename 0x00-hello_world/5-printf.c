#include <stdio.h>

/**
 * main - Print text using the printf function
 *
 * Return: 0 if all statements are executed successfully
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
