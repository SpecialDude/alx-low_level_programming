#include "main.h"

/**
 * _abs - Compute the absolute value of an integer
 *
 * @n: Number
 *
 * Return: absolue Value of number (int)
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -1);

	return (n);
}
