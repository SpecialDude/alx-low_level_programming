#include <stdio.h>

/**
 * main - Print Test to the console
 *
 * Return - 0 success
 */
int main(void)
{
	const char* text = "_putchar";

	for (int i = 0; i < 8; i++)
	{
		putchar(text[i]
);
	}
	putchar('\n');

	return (0);
}
